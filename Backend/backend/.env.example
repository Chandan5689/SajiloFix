# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/sajilofix

# Supabase Settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Email Configuration (Gmail Example)
# For Gmail: You need to create an App Password
# 1. Go to Google Account Settings > Security
# 2. Enable 2-Step Verification
# 3. Generate App Password for "Mail"
# 4. Use that password here (not your regular Gmail password)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=SajiloFix <your-email@gmail.com>

# For development/testing (console backend - emails print to console)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Firebase (optional)
FIREBASE_SERVICE_ACCOUNT_PATH=path/to/firebase-service-account.json
